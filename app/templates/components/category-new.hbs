{{#if addNewCategory}}
<h1>New Category</h1>
  <div>
    <form>
      <div class="form-group">
        <label for="description">description</label>
        {{input value=description id="description"}}
      </div>

      <div class="form-group">
        <label for="author">Add Image</label>
        {{input value=category_image id="category_image"}}
      </div>
      <button {{action 'saveCategory'}}>Save</button>
    </form>
  </div>
 {{else}}
    <button class="btn btn-success"{{action 'categoryFormShow'}}>New question</button>
{{/if}}
